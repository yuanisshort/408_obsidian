![图片说明](D:\学习资料\考研\操作系统\8855221522228282\214488858184251.jpg)
您好 第12题麻烦讲一下 每个页框 128个字  意思是一次读取数组两行    第一行是a00到a0 63 第二行是 a10到a1 63吗这样的话不就只会发生32次中断，答案是64*64/2=2048
仔细读题，程序中的i和j的变化，是按列访问数组元素的。每次缺页可以读进连续的两行数组元素（128个字），因此是64*64/2=2048。![图片说明](D:\学习资料\考研\操作系统\8855221522228282\214488858184251.jpg)
您好 第12题麻烦讲一下 每个页框 128个字  意思是一次读取数组两行    第一行是a00到a0 63 第二行是 a10到a1 63吗这样的话不就只会发生32次中断，答案是64*64/2=2048
仔细读题，程序中的i和j的变化，是按列访问数组元素的。每次缺页可以读进连续的两行数组元素（128个字），因此是64*64/2=2048。