![图片说明](D:\学习资料\考研\操作系统\4844284885821218\412224152211218.jpg)
您好 只能上传宇一个图片  这道题题意是 有n个销售员  销售员空闲才会叫号 顾客取号  题目要求设置两个整型变量 我这里j表示叫号  我的代码部分销售员写的跟答案不太一样  我是判断当前叫号小于已经取的号 再进行p操作 我感觉我这样不用写else的情况  同时我写了一个semaphore定义的m等于n表示空闲的销售员的数量 但是答案根本就没写销售员 请问您看我这个销售员这样写对吗
多个销售员的话，你的答案是错的，因为你的 if(j<qu)成功之后，万一另一个销售员刚刚让j加1了，使得j>=qu了，那当前这个进程的后序操作就不应该进行了。所以需要先P(mutex_j)。记住只要是读写的变量可能被另一个进程改变，就需要先取得互斥量再进行后续的操作。![图片说明](D:\学习资料\考研\操作系统\4844284885821218\412224152211218.jpg)
您好 只能上传宇一个图片  这道题题意是 有n个销售员  销售员空闲才会叫号 顾客取号  题目要求设置两个整型变量 我这里j表示叫号  我的代码部分销售员写的跟答案不太一样  我是判断当前叫号小于已经取的号 再进行p操作 我感觉我这样不用写else的情况  同时我写了一个semaphore定义的m等于n表示空闲的销售员的数量 但是答案根本就没写销售员 请问您看我这个销售员这样写对吗
多个销售员的话，你的答案是错的，因为你的 if(j<qu)成功之后，万一另一个销售员刚刚让j加1了，使得j>=qu了，那当前这个进程的后序操作就不应该进行了。所以需要先P(mutex_j)。记住只要是读写的变量可能被另一个进程改变，就需要先取得互斥量再进行后续的操作。