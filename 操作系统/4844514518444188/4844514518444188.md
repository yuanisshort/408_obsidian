![图片说明](D:\学习资料\考研\操作系统\4844514518444188\181151514281882.jpg)
哥，我有一个问题，就是根据王道书上说，大概是这样，页框大小为512B.进程就是访问一个整形二维数组，128行128列，意味着一行刚好能全部放到一个页框，如果像我这样访问，就意味着只需要发生128次缺页中断对吧，我有一个问题就是，每行开头发生缺页中断调入一行，这是不是就是所谓的局部性原理啊，同理，是不是页框大小为1024就只需要每两行发生一次缺页中断
1. 你的第一行代码不对，应该是 int a[128][128]
2. 你的代码确实只发生128次缺页中断，是局部性原理
3. 页框大小为1024B，确实每访问两行发生一次缺页中断![图片说明](D:\学习资料\考研\操作系统\4844514518444188\181151514281882.jpg)
哥，我有一个问题，就是根据王道书上说，大概是这样，页框大小为512B.进程就是访问一个整形二维数组，128行128列，意味着一行刚好能全部放到一个页框，如果像我这样访问，就意味着只需要发生128次缺页中断对吧，我有一个问题就是，每行开头发生缺页中断调入一行，这是不是就是所谓的局部性原理啊，同理，是不是页框大小为1024就只需要每两行发生一次缺页中断
1. 你的第一行代码不对，应该是 int a[128][128]
2. 你的代码确实只发生128次缺页中断，是局部性原理
3. 页框大小为1024B，确实每访问两行发生一次缺页中断