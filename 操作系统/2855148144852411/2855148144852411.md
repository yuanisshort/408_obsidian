![图片说明](D:\学习资料\考研\操作系统\2855148144852411\411828114412558.jpg)
关闭文件时到底需要不需要写磁盘？写回去的是数据信息还是控制信息？延迟写和写文件操作和这个有什么关系？
选B，一般情况下【关闭文件的时候】是需要写磁盘的，如果内容未发生更改，会更新一些控制信息比如最近一次的打开时间，如果发生了更改，那么会更新文件的内容（数据）。

题目问【不需要的操作】那就是【100%不需要的操作】，其他选项都可能发生。

你画出来的绿色的解释是错误的，例如Linux系统的关闭文件系统调用fclose就会写磁盘（看图）。

磁盘延迟写（Disk Deferred Write）是一种优化技术，用于提高磁盘I/O性能。在这种机制下，系统并不会立即将数据写入磁盘，而是将数据先写入内存（通常是缓存或缓冲区）。系统可以在合适的时机（如缓冲区满、系统空闲时或定时）将这些数据批量写入磁盘，从而减少频繁的磁盘I/O操作，可以显著提高系统的整体性能，尤其是在高负载情况下。![图片说明](D:\学习资料\考研\操作系统\2855148144852411\411828114412558.jpg)
关闭文件时到底需要不需要写磁盘？写回去的是数据信息还是控制信息？延迟写和写文件操作和这个有什么关系？
选B，一般情况下【关闭文件的时候】是需要写磁盘的，如果内容未发生更改，会更新一些控制信息比如最近一次的打开时间，如果发生了更改，那么会更新文件的内容（数据）。

题目问【不需要的操作】那就是【100%不需要的操作】，其他选项都可能发生。

你画出来的绿色的解释是错误的，例如Linux系统的关闭文件系统调用fclose就会写磁盘（看图）。

磁盘延迟写（Disk Deferred Write）是一种优化技术，用于提高磁盘I/O性能。在这种机制下，系统并不会立即将数据写入磁盘，而是将数据先写入内存（通常是缓存或缓冲区）。系统可以在合适的时机（如缓冲区满、系统空闲时或定时）将这些数据批量写入磁盘，从而减少频繁的磁盘I/O操作，可以显著提高系统的整体性能，尤其是在高负载情况下。