感觉计网中“可靠性”这个概念很模糊，有连接不一定可靠，但是无连接一定不可靠吗？哪些是可靠传输，哪些是不可靠传输？
"可靠性"是指在数据传输过程中确保数据的正确、完整地到达目标的特性。和其他因素无关。可靠的协议有TCP等，不可靠的有UDP等。

虽然无连接的传输方式（如UDP）在某种程度上被认为是不可靠的，但这并不意味着无连接传输方式无法传输可靠的数据。应用程序可以在UDP之上实现自己的机制，例如应用层协议，来确保数据的可靠性。因此，可靠性与传输层协议的选择并不是绝对关联的，而取决于应用程序和实际需求。