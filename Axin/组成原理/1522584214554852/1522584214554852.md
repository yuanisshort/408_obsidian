学长，请问为什么在主存容量拓展那里，线选法为什么会导致地址空间不连续，而片选法不会嘞
线选法是直接用地址线连接存储芯片，当某地址线信息为“0”时，就选中与之对应的存储芯片。

看图，比如选择芯片0，地址信号高位是1110，而选择芯片1的地址信号是1101，明显不连续（1101过了是1110才对）。

如果用片选法（如使用2-4译码器或3-8译码器进行译码）那么各个芯片之间的地址就是连续的了。比如输入的地址信号为00译码为1110，01译码为1101,10译码为1011,11译码为0111；这样就保证了片选时输入的地址是连续的。（看王道计组104页和103页的图）