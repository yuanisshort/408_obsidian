有个疑问就是指令寻址那里，假如汇编指令是jmp后面加一串地址，是相对寻址还是直接寻址，我看王道书上讲的是相对寻址，感觉和直接寻址一样
相对寻址是一种根据指令本身的偏移量或相对位置来计算目标地址的寻址方式。

在相对寻址中，jmp指令中的地址表示跳转目标相对于当前指令的偏移量。当执行jmp指令时，处理器会将当前指令的地址（PC）与偏移量相加，得到跳转目标的地址。例如 jmp 0 表示跳转到下一条指令（因为执行jmp之前PC已经加一）

与之相对的是直接寻址（Direct Addressing），在直接寻址中，指令中的地址直接指定了目标操作数或跳转目标的地址。