这题我这样写可以吗？
我是这样想的：矩阵经过初等变换后是等价的，然后A就转换为很简单的对角矩阵，然后再求它的逆矩阵和行列式。
行列式的值可能不对，是不是不能从矩阵直接化为行列式来求值，而应该把一开始的矩阵化为行列式？
也就是经过n-1次列变换变成我写的这个行列式，前面就可以加上（-1）^n-1，跟答案很像了。但是不知道为什么还是跟答案不一样。
等价矩阵的逆矩阵并不是原矩阵的逆矩阵，你这道题是不是直接把A的等价矩阵的逆矩阵当做A的逆矩阵了