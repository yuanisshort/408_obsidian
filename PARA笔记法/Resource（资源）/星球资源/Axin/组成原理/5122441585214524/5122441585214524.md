![图片说明](D:\学习资料\考研\组成原理\5122441585214524\188522811444142.jpg)
第四问的解法2没看懂  平均每次访存成功的时间为什么不乘0.95啊，
这是先算的一条指令的平均开销再乘以100，Cache缺失时，先从内存取到Cache，再从Cache到CPU。这里是分两部分计算的，第一部分是每条指令命中时的开销（这部分100条指令都是一样的，都是从Cache到CPU），第二部分是平均每条指令Cache缺失的额外开销。