分组在OSI参考模型中到底是层与层之间到底是上层服务下层还是下层服务上层？
王道书上写的“数据链路层将网络层交来的IP分组封装成帧”，也就是说网络层向数据链路层提交数据，网络层的PDU变成数据链路层的SDU，也就是网络层服务于数据链路层，但是书上又说是“下一层为上一层提供服务”，到底数据是谁向谁提交😰层次之间谁向谁服务啊
下层为上层提供服务。被封装的层就是被服务的层。提供封装功能的层，就是服务的层。所以相邻的层是下层服务上层。计算机里面都是越接近物理硬件的为越高级的提供服务。